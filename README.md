A simple application built using Swift (UIKit framework) to manage user information 

Future updates:
1. Fix truncating label name in the view user page
2. Work on perfecting UI with different orientations
